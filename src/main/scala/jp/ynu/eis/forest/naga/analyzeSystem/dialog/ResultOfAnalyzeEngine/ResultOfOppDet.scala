package jp.ynu.eis.forest.naga.analyzeSystem.dialog.ResultOfAnalyzeEngine

import org.aiwolf.common.data.Agent

import scala.collection.mutable

class ResultOfOppDet {
  var kanoList = mutable.MutableList.empty[Agent]
  var keldicList = mutable.MutableList.empty[Agent]
  var mcreList = mutable.MutableList.empty[Agent]
  var wordWolfList = mutable.MutableList.empty[Agent]
  var indigoList = mutable.MutableList.empty[Agent]
  var udonList = mutable.MutableList.empty[Agent]
  var rosenList = mutable.MutableList.empty[Agent]
  var fukuList = mutable.MutableList.empty[Agent]
  var ariesList = mutable.MutableList.empty[Agent]
  var cainsList = mutable.MutableList.empty[Agent]

  var kano = Option.empty[Agent]
  var keldic = Option.empty[Agent]
  var mcre = Option.empty[Agent]
  var wordWolf = Option.empty[Agent]
  var indigo = Option.empty[Agent]
  var udon = Option.empty[Agent]
  var rosen = Option.empty[Agent]
  var fuku = Option.empty[Agent]
  var aries = Option.empty[Agent]
  var cains = Option.empty[Agent]
}
